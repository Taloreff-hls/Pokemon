export const TIMEOUT_SHORT_DURATION = parseInt(
  import.meta.env.VITE_TIMEOUT_SHORT_DURATION || "1000"
);

export const TIMEOUT_MEDIUM_DURATION = parseInt(
  import.meta.env.VITE_TIMEOUT_MEDIUM_DURATION || "2000"
);

export const TIMEOUT_LONG_DURATION = parseInt(
  import.meta.env.VITE_TIMEOUT_LONG_DURATION || "3000"
);
